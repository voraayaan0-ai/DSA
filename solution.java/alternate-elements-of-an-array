class Solution {
    public ArrayList<Integer> getAlternates(int arr[]) {
        ArrayList<Integer> result = new ArrayList<>();
        int n = arr.length;
       for(int j = 0; j<n; j += 2){
           result.add(arr[j]);
       }
       return result;
    }
}
